_schema-version: "3.2"
ID: AppLists1
version: 0.0.1
modules:
- name: applists1-approuter
  type: approuter.nodejs
  path: applists1-approuter
  requires:
  - name: AppLists1_html_repo_runtime
  parameters:
    disk-quota: 256M
    memory: 256M
- name: AppLists1_ui_deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: AppLists1_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - Lists1-content.zip
      name: Lists1
      target-path: resources/
- name: Lists1
  type: html5
  path: Lists1
  build-parameters:
    builder: custom
    commands:
    - npm run build
    supported-platforms: []
resources:
- name: AppLists1_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: AppLists1_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm install
